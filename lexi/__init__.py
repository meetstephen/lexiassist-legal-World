"""LexiAssist package — modular split of the original single-file app.

This package contains the same code that previously lived in `app.py`,
now organised into modules. See `app.py` at the repo root for the entry
point. The split is purely mechanical — no behaviour changes.
"""
from __future__ import annotations
